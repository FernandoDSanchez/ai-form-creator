# The user needs CREATE DATABASE permission: the Temporal auto-setup image
# creates `temporal` and `temporal_visibility` at first boot.
POSTGRES_DB=temporal
POSTGRES_USER=temporal
POSTGRES_PASSWORD=
