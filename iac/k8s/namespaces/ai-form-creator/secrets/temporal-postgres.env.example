# El usuario necesita permiso de CREATE DATABASE: la imagen auto-setup de
# Temporal crea `temporal` y `temporal_visibility` en el primer arranque.
POSTGRES_DB=temporal
POSTGRES_USER=temporal
POSTGRES_PASSWORD=
